import 'package:flutter/material.dart';

import 'package:front_lomba/model/models.dart';

/*class AppBarSection extends PreferredSizeWidget {

  final String title;

   AppBarSection({
    Key? key,  
    required this.title,
  }) : super(key: key, preferredSize: Size.fromHeight(50));


  @override
  Widget build(BuildContext context) {
    return AppBar(
        title: Text(title),
        actions: [
          IconButton(
            icon: const Icon(Icons.account_circle_rounded),
            tooltip: 'Perfil',
            onPressed: () {
              Navigator.push(context, MaterialPageRoute(builder: (context) => Profile()));
            },
          )
        ]
      );
  }
}*/